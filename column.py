class column:
    def __init__(self, rows, colName, prevC, nextC):
       self.colName = colName;
       self.rows = rows
       self.prev = prevC
       self.next = nextC
    def tentativeClassification(tc):
       self.tentClass = tc;
